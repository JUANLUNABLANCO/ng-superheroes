import { BoldFirstLetterPipe } from './bold-first-letter.pipe';

describe('BoldFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
